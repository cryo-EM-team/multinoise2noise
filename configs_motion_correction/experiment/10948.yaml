# @package _global_
defaults:
  - override /datamodule: "10948"
  - override /trainer: cpu

core:
  group: extraction

tags: ['motion_correction', 'relion', '10948', 'real']

task: motion_correction_10948
