# @package _global_
defaults:
  - override /datamodule: "10028"
  - override /trainer: cpu

core:
  group: extraction

tags: ['motion_correction', 'relion', '10028', 'real']

task: motion_correction_10028
