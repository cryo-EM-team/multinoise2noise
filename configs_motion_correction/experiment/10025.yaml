# @package _global_
defaults:
  - override /datamodule: "10025"
  - override /trainer: cpu

core:
  group: extraction

tags: ['motion_correction', 'relion', '10025', 'real']

task: motion_correction_10025
