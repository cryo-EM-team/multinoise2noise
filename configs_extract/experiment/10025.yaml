# @package _global_
defaults:
  - override /datamodule: "10025"
  - override /trainer: cpu

core:
  group: extraction

lightning_model:
  extractor:
    ctf_mode: "sign"

tags: ['extraction', 'relion', '10025', 'real']

task: extraction_10025
