# @package _global_
defaults:
  - override /datamodule: "10028"
  - override /trainer: cpu

core:
  group: extraction

lightning_model:
  extractor:
    ctf_mode: "sign"

tags: ['extraction', 'relion', '10028', 'real']

task: extraction_10028
