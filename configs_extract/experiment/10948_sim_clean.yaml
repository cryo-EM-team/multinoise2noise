# @package _global_
defaults:
  - override /datamodule: "10948_sim"
  - override /trainer: cpu

core:
  group: extraction

datamodule:
  out_dir:  "${datamodule.dataset_path}/extraction/clean"
  particles_star_path: "${datamodule.dataset_path}/${datamodule.dataset_name}_extract_clean.star"
  num_frames: 1

tags: ['extraction', 'relion', '10948', 'simulated']

task: extraction_10948_sim
