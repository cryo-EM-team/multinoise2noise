# @package _global_
defaults:
  - override /datamodule: "10474_sim"
  - override /trainer: cpu

core:
  group: extraction

lightning_model:
  extractor:
    ctf_mode: "sign"

datamodule:
  out_dir:  "${datamodule.dataset_path}/extraction/clean"
  particles_star_path: "${datamodule.dataset_path}/${datamodule.dataset_name}_clean_extract.star"
  num_frames: 1

tags: ['extraction', 'relion', '10474', 'simulated']

task: extraction_10474_sim
