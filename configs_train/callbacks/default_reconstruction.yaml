defaults:
  - model_checkpoint
  - model_summary
  - rich_progress_bar
  - early_stopping
  - lr_monitor
  - epoch_informer
  - _self_

early_stopping:
  monitor: "val/loss" # quantity to be monitored, must be specified !!!
<<<<<<< HEAD
  patience: 7 # number of checks with no improvement after which training will be stopped
=======
  patience: 9 # number of checks with no improvement after which training will be stopped
>>>>>>> 5d392be (fixed batch size)
  verbose: True # verbosity mode
  mode: "min"
  strict: False

model_checkpoint:
  dirpath: ${paths.output_dir}/checkpoints/${datamodule.dataset_name}
  filename: "epoch_{epoch:03d}"
  monitor: "val/loss"
  mode: "min"
  save_last: True
  auto_insert_metric_name: False

model_summary:
  max_depth: -1
